<% unless flash.empty? %>
  <script>
      <% flash.each do |f| %>
      <% type=f[0].to_s.gsub('alert', 'red').gsub('warning', 'orange').gsub('notice', 'green') %>
      M.toast({html: '<%= f[1] %>', classes: '<%= type %>'});
      <% end %>
  </script>
<% end %>